
<%= bootstrap_form_with model: [ @post, @post.comments.build ] do |form| %>
  <%= form.hidden_field :user_id, value: current_user.id %>
  <%= form.hidden_field :post_id, value: @post.id %>
  <%= form.text_area :content %>
  <div class="actions">
    <%= form.submit %>
    </div>
<% end %>
